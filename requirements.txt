# RAPIDS GPU packages (cudf, cuml, xgboost, cupy, pandas, numpy, pyarrow,
# scikit-learn, rich, click, fastapi) are pre-installed in the base image.
# Do NOT reinstall them via pip to avoid CUDA version conflicts.

# API & Serving (uvicorn not in base image)
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Experiment Tracking
mlflow>=2.10.0

# Hyperparameter Tuning
optuna>=3.5.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.26.0

# Notebooks
jupyterlab>=4.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
